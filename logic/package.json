{
  "name": "parking-logic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "gen": "tsc -p tsconfig.gen.json && node ../.tmp/gen/tools/gen-levels.js",
    "typecheck:view": "tsc -p tsconfig.view.json",
    "preview": "node ../tools/preview.mjs",
    "preview:nobuild": "node ../tools/preview.mjs --no-build"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  }
}
